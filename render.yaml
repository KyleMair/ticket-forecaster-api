services:
  - type: web
    name: ticket-forecaster-api
    runtime: python
    pythonVersion: "3.11.8"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
